apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: express-api-prod

namePrefix: prod-

resources:
  - ../../base

patches:
  - path: patches/deployment.yaml
  - path: patches/configmap.yaml
  - path: patches/ingress.yaml
  - path: patches/hpa.yaml
